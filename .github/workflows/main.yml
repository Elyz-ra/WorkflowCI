name: MLflow CI/CD Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-train:
    runs-on: ubuntu-latest
    environment: Production
    env: # Global environment variables for this job
      PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python # For protobuf compatibility

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: 3.9
          miniconda-version: "latest"
          auto-activate-base: false
          channels: conda-forge,defaults

      - name: Check Env
        run: |
          python --version
          conda --version
          pip --version
        shell: bash -l {0}

      - name: Install dependencies (Conda Environment)
        run: |
          # Ensure a clean environment by removing it if it exists
          conda env remove --name mlflow-ci-env || true
          conda env create -f MLProjects/conda.yaml
          conda activate mlflow-ci-env
        shell: bash -l {0}

      - name: Start MLflow Tracking Server (Local)
        run: |
          conda activate mlflow-ci-env
          # Use 'python -m mlflow' for consistency
          python -m mlflow server --host 127.0.0.1 --port 5001 --backend-store-uri sqlite:///mlruns.db --default-artifact-root ./mlruns &
          
          echo "Waiting for MLflow Server to start..."
          timeout_seconds=60
          start_time=$(date +%s)
          while ! curl -s http://127.0.0.1:5001 > /dev/null; do
            current_time=$(date +%s)
            elapsed=$((current_time - start_time))
            if [ "$elapsed" -ge "$timeout_seconds" ]; then
              echo "MLflow Server did not start within $timeout_seconds seconds."
              exit 1
            fi
            echo "MLflow Server not yet ready. Waiting 5 seconds..."
            sleep 5
          done
          echo "MLflow Server is running and ready!"
        shell: bash -l {0}

      - name: Run MLflow Project
        run: |
          conda activate mlflow-ci-env
          export MLFLOW_TRACKING_URI="http://127.0.0.1:5001"
          export MLFLOW_REGISTRY_URI="http://127.0.0.1:5001"
          # Use 'python -m mlflow' for consistency
          python -m mlflow run MLProjects -e main --experiment-name "Shipping Delay Prediction"
        shell: bash -l {0}

      - name: Get latest MLflow run_id
        id: get_run_id
        run: |
          conda activate mlflow-ci-env
          export MLFLOW_TRACKING_URI="http://127.0.0.1:5001"
          export MLFLOW_REGISTRY_URI="http://127.0.0.1:5001"
          python -m mlflow --version # Diagnostic: Check active MLflow version
          # Use 'python -m mlflow search runs' for reliability
          LATEST_RUN_ID=$(python -m mlflow search runs --experiment-name "Shipping Delay Prediction" --max-results 1 --order-by "start_time DESC" --query "tags.mlflow.runName IS NOT NULL" --output-format json | jq -r '.[0].info.run_id')
          echo "latest_run_id=$LATEST_RUN_ID" >> $GITHUB_OUTPUT
        shell: bash -l {0}

      - name: Save MLflow Run Data as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-run-artifacts-${{ github.run_id }}
          path: mlruns/${{ steps.get_run_id.outputs.latest_run_id }}/artifacts/

      - name: Build Docker Model
        run: |
          conda activate mlflow-ci-env
          export MLFLOW_TRACKING_URI="http://127.0.0.1:5001"
          export MLFLOW_REGISTRY_URI="http://127.0.0.1:5001"
          # Use 'python -m mlflow' for consistency
          python -m mlflow models build-docker --model-uri "models:/ShippingDelayXGBoostModel/Production" -n shipping-delay-xgb-model:latest
        shell: bash -l {0}

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag Docker Image
        run: |
          DOCKER_IMAGE_NAME="shipping-delay-xgb-model"
          DOCKER_REPO="${{ secrets.DOCKER_USERNAME }}/$DOCKER_IMAGE_NAME"
          docker tag $DOCKER_IMAGE_NAME:latest $DOCKER_REPO:latest
          docker tag $DOCKER_IMAGE_NAME:latest $DOCKER_REPO:${{ github.run_id }}
        shell: bash -l {0}

      - name: Push Docker Image
        run: |
          DOCKER_IMAGE_NAME="shipping-delay-xgb-model"
          DOCKER_REPO="${{ secrets.DOCKER_USERNAME }}/$DOCKER_IMAGE_NAME"
          docker push $DOCKER_REPO:latest
          docker push $DOCKER_REPO:${{ github.run_id }}
        shell: bash -l {0}
